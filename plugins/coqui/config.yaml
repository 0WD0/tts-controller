image: tts-coqui:latest
environment:
  MODEL_NAME: tts_models/en/ljspeech/tacotron2-DDC
  DEVICE: cuda
volumes:
  - /data/models:/app/models
  - /data/cache:/app/cache
ports:
  5001: 5001  # host_port: container_port
